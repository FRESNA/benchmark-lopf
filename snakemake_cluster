#!/bin/bash

snakemake --jobscript jobscript.sh --cluster-config cluster.yaml --cluster "sbatch -p {cluster.partition} -n 1 --cpus-per-task {cluster.cpus} -o {cluster.output} --mem {resources.mem}" "$@"
